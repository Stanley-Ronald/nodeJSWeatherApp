<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Stan's Weather</title>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
  </head>
  <body>
  <h1>Stan's Current Weather Service</h1>
  <h3>Get started by entering your city below!</h3>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" placeholder="Enter a City" required>
          <input type="submit" value="Get Current Weather">
        </form>
		 
		
        <% if(weather !== null){ %>
          <p><%- weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>